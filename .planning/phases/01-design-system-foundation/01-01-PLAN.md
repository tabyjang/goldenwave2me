---
phase: 01-design-system-foundation
plan: 01
type: execute
domain: ui-ux-pro-max
---

<objective>
Design System Foundation - 새로운 브랜드 컬러, 타이포그래피, CSS 변수를 globals.css에 정의

Purpose: 현재 무채색(흑백) 스킴을 UI UX Pro Max 추천 디자인 시스템으로 전환
Output: 업데이트된 globals.css + 폰트 설정
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system/goldenwave/MASTER.md

**Design System Reference (Source of Truth):**
- Primary: #EC4899 (Pink)
- Secondary: #F472B6 (Light Pink)
- CTA/Accent: #06B6D4 (Cyan)
- Background: #FDF2F8 (Warm Pink White)
- Text: #831843 (Dark Pink)
- Font: Plus Jakarta Sans

**Current files to modify:**
@src/app/globals.css
@src/app/layout.tsx
@tailwind.config.ts (if exists)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CSS color variables in globals.css</name>
  <files>src/app/globals.css</files>
  <action>
현재 무채색(oklch 0 채도) 컬러 변수를 새 디자인 시스템 컬러로 교체:

**:root (Light mode):**
- --background: oklch(0.98 0.01 350) /* #FDF2F8 근사값 */
- --foreground: oklch(0.25 0.08 350) /* #831843 근사값 */
- --primary: oklch(0.65 0.25 350) /* #EC4899 근사값 */
- --primary-foreground: oklch(1 0 0) /* white */
- --secondary: oklch(0.75 0.15 350) /* #F472B6 근사값 */
- --accent: oklch(0.7 0.15 195) /* #06B6D4 근사값 */
- --muted: oklch(0.95 0.01 350)
- --muted-foreground: oklch(0.45 0.05 350)

**새 변수 추가:**
- --cta: oklch(0.7 0.15 195) /* CTA 버튼용 */
- --gradient-start: oklch(0.65 0.25 350)
- --gradient-end: oklch(0.7 0.15 195)

**.dark 모드도 적절히 조정** (어두운 배경에 맞게)

OKLCH 사용 이유: Tailwind CSS 4 네이티브, 색상 조작 용이
  </action>
  <verify>브라우저에서 색상이 올바르게 렌더링되는지 확인 (개발 서버 실행)</verify>
  <done>모든 컬러 변수가 새 디자인 시스템 값으로 업데이트됨</done>
</task>

<task type="auto">
  <name>Task 2: Add Plus Jakarta Sans font</name>
  <files>src/app/layout.tsx, src/app/globals.css</files>
  <action>
**layout.tsx 수정:**
1. next/font/google에서 Plus_Jakarta_Sans import
2. 폰트 설정: weights [300, 400, 500, 600, 700], subsets ['latin']
3. className을 body에 적용

**globals.css 수정:**
1. --font-sans 변수를 Plus Jakarta Sans로 설정
2. 기존 Geist 폰트 참조 유지 (fallback)

**코드 예시:**
```tsx
import { Plus_Jakarta_Sans } from 'next/font/google'

const plusJakarta = Plus_Jakarta_Sans({
  subsets: ['latin'],
  weight: ['300', '400', '500', '600', '700'],
  variable: '--font-plus-jakarta',
})
```

주의: 기존 Geist 폰트도 유지하되 Plus Jakarta를 primary로
  </action>
  <verify>npm run dev 후 브라우저 개발자 도구에서 폰트 적용 확인</verify>
  <done>Plus Jakarta Sans가 기본 폰트로 적용됨</done>
</task>

<task type="auto">
  <name>Task 3: Add gradient and effect utility classes</name>
  <files>src/app/globals.css</files>
  <action>
**@layer base에 추가:**
```css
/* Gradient Text */
.gradient-text {
  background: linear-gradient(135deg, var(--primary), var(--accent));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* Aurora Gradient Background */
.aurora-bg {
  background: linear-gradient(
    135deg,
    oklch(0.98 0.01 350) 0%,
    oklch(0.95 0.05 350) 50%,
    oklch(0.92 0.08 195) 100%
  );
}

/* Glass Card Effect */
.glass-card {
  background: oklch(1 0 0 / 0.7);
  backdrop-filter: blur(12px);
  border: 1px solid oklch(1 0 0 / 0.2);
}

/* Dark mode variants */
.dark .glass-card {
  background: oklch(0.2 0 0 / 0.7);
  border: 1px solid oklch(1 0 0 / 0.1);
}
```

**추가 유틸리티:**
- .hover-lift: transform + shadow 호버 효과
- .transition-smooth: transition-all duration-200 ease
  </action>
  <verify>클래스들이 Tailwind와 충돌 없이 적용되는지 확인</verify>
  <done>그라데이션, 글래스 효과 유틸리티 클래스 추가됨</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>새 디자인 시스템 컬러, 폰트, 유틸리티 클래스</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:3000
    3. 확인사항:
       - 전체 색상이 핑크/시안 계열로 변경됨
       - 폰트가 Plus Jakarta Sans로 적용됨 (개발자 도구 > Computed > font-family)
       - 다크 모드 토글 시 색상이 적절히 조정됨
    4. 추가 확인:
       - 텍스트 가독성 (대비 충분한지)
       - 기존 컴포넌트들이 깨지지 않음
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] 컬러 변수가 올바르게 적용됨
- [ ] Plus Jakarta Sans 폰트 로드됨
- [ ] 그라데이션 유틸리티 클래스 작동
- [ ] 라이트/다크 모드 모두 적절한 대비
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript/CSS errors
- 디자인 시스템 기초가 globals.css에 구현됨
- 다음 Phase(Hero Redesign)를 위한 기반 완성
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-01-SUMMARY.md`
</output>
